rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny all — rules added per story
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
